{
  "name": "aarogyavaani-medikiosk",
  "version": "2.0.0",
  "private": true,
  "description": "AarogyaVaani · MediKiosk — AI-assisted patient case-taking platform for AYUSH and government OPDs (SIH26047)",
  "type": "module",
  "scripts": {
    "start": "node combined-start.mjs",
    "dev": "node server.mjs",
    "build": "node --check data.js && node --check views.js && node --check main.js && npm --prefix nirogaverse/server install --include=dev && npm --prefix nirogaverse/server run prisma:generate && npm --prefix nirogaverse/server run build && npm --prefix nirogaverse/client install && npm --prefix nirogaverse/client run build"
  },
  "engines": { "node": ">=18" }
}
